#include <stdio.h>
#include <conio.h>
void table();
int main()
{
    table();
    getch();
    return 0;
}
void table()
{
    int i=1,n;
    printf("enter a number");
    scanf("%d",&n);
    for(i=1;i<=10;i++)
    printf("%d*%d=%d\n",n,i,n*i);
}
