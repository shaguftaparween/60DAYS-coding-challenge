#include <stdio.h>
#include<stdio.h>
int main()
{
    int i,j,a,b;
    printf("enter no of row and coloumn\n");
    scanf("%d%d",&a,&b);
    for(i=1;i<=a;i++)//row
    {
     for(j=1;j<=b;j++)//column
    {
      printf("*");
     }
     printf("\n");
}
return 0;
}
