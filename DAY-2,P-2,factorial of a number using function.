#include<stdio.h>
#include<conio.h>
int fact(int n);
int main()
{
    int n,f;
    printf("enter a number");
    scanf("%d",&n);
    f=fact(n);
    printf("factorial of this no is %d",f);
    getch();
    return 0;
}
int fact(int n)
{
    int i,f=1;
    for(i=1;i<=n;i++)
    f=f*i;
    return f;
}
