#include <stdio.h>
#include <conio.h>
void revodd();
int main()
{
    revodd();
    getch();
    return 0;
}
void revodd()
{
    int n,i=n;
    printf("enter a number");
    scanf("%d",&n);
    for(i=n;i<=n;i--)
    {
    if(i%2==!0)
    printf("%d\n",i);
  
    }

}
