#include <stdio.h>
#include<conio.h>
void natural(int n);
int main()
{
    int k;
    printf("enter a number");
    scanf("%d",&k);
    natural(k);
}
void natural(int n)
{
    
    if(n>0)
    printf("%d\n",n);
    natural(n-1);
    
}
