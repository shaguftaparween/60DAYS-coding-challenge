#include <stdio.h>
#include<conio.h>
void natural(int n);
int main()
{
    int k;
    printf("enter a number");
    scanf("%d",&k);
    natural(k);
}
void natural(int n)
{
    //int n;
    if(n>1)
    natural(n-1);
    printf("%d\n",n);
}
